//common use case
function loadVideos(pageNumber, numberOfVideos) {
  //call api and get data
  return videos;
}

function displayVideos(videos) {
  //get html elements on the page and fill in data
  //div element innerText = video info
}

function loadShorts() {
  //call api and get data
  return shorts;
}

function displayShorts(shorts) {
  //get html elements on the page and fill in data
  //div element innerText = short info
}

//loadSubscriptions, displaySubscriptions
//loadTags, displayTags
